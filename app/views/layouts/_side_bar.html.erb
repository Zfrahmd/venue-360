<nav class="col-lg-2 col-md-3 col-xs-12 sidebar d-flex flex-column ">
  <div class="profile-section pt-3 px-4">
      <div class="d-flex align-items-center">
        <div class="p-2">
          <h4 class="logo-text fw-bold mb-0">
            <% if current_user.admin?%>
              Admin 
            <% else %>
              User 
            <% end %>   
          </h4>
          <p class="text-white small hide-on-collapse">Dashboard</p>
        </div>
    </div>
    </div>
  <!--div class="profile-section mt-auto p-4"-->

  <div class="nav flex-column">
    <% if current_user %>
        <%= link_to user_profile_path(current_user.id), class: "sidebar-link active text-decoration-none p-3" do %>
          <i class="fas fa-home me-3"></i>
          <span class="hide-on-collapse">Dashboard</span>
        <% end %>
      <a href="#" class="">
        <%= link_to inquiries_path, class: "sidebar-link text-decoration-none p-3" do %>
          <i class="fas fa-commenting me-3"></i>
          <span class="hide-on-collapse">Inquiries</span>
        <% end %>
      <%= link_to new_inquiry_path, class: "sidebar-link text-decoration-none p-3" do %>
        <i class="fas fa-calendar-check me-3"></i>
        <span class="hide-on-collapse">Reservations</span>
      <% end %>
      <% if current_user.admin?%>
        <a href="#" class="sidebar-link text-decoration-none p-3">
          <i class="fas fa-users me-3"></i>
          <span class="hide-on-collapse">Users</span>
        </a>
        <a href="#" class="sidebar-link text-decoration-none p-3">
          <i class="fas fa-gear me-3"></i>
          <span class="hide-on-collapse">Settings</span>
        </a>
        <div class="rooms">
          <div class="sidebar-link p-3">  
            <i class="fas fa-users me-3"></i>
            <button class="dropdown-toggle sidebar-collapsible-button" data-bs-toggle="collapse" data-bs-target="#rooms-collapse" aria-expanded="false">
              Rooms
            </button>
          </div>
          <div class="collapse" id="rooms-collapse" style="">
            <ul class="px-3">
              <li><%= link_to "Add New Listing", new_room_path(current_user.id), class: "sidebar-link-drop-item" %></li>
              <li><%= link_to "Manage", manage_rooms_path, class: "sidebar-link-drop-item" %></li>
            </ul>
          </div>
        </div>
        <div class="amenities">
          <div class="sidebar-link p-3">
            <i class="fas fa-users me-3"></i>
            <button class="dropdown-toggle sidebar-collapsible-button" data-bs-toggle="collapse" data-bs-target="#account-collapse" aria-expanded="false">
              Amenities
            </button>
          </div>
          <div class="collapse" id="account-collapse" style="">
            <ul class="px-3">
              <li><%= link_to "Create Amenity", new_amenity_path(), class: "sidebar-link-drop-item" %></li>
              <li><%= link_to "Manage", amenities_path, class: "sidebar-link-drop-item" %></li>
            </ul>
          </div>
        </div>
      <% else %>
      <% end %>
    <% end %>  
  </div>  
</nav>